export enum EButtonStyle {
  primary = 'primary',
  secondary = 'secondary',
  muted = 'muted',
  text = 'text',
  loading = 'loading',
}

export enum EButtonSize {
  fixed = 'fixed',
  large = 'large',
  small = 'small',
}

export enum EButtonIcon {
  left = 'left',
  right = 'right',
  counter = 'counter',
  none = 'none'
}
